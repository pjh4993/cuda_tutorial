#ifndef VECTOR_SUM_HDR
#define VECTOR_SUM_HDR

#include "../build/chapter_4_export.h"

CHAPTER_4_EXPORT int vector_sum(void);

template<typename T>
CHAPTER_4_EXPORT void add(T *a, T *b, T *c, T S);

#endif